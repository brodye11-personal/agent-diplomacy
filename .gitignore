# Environment / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/

# Game logs — KEEP the structured JSONL: it is the experiment's data and the
# planned website's backend, and it is EXPENSIVE to regenerate (paid API runs),
# not "regenerable". Ignore only transient crash-recovery checkpoints.
logs/*.checkpoint.json
*.log

# Claude Code per-user state
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Linked git worktrees (one folder per branch for parallel Cursor windows)
worktrees/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
