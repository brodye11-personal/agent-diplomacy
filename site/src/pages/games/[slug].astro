---
import SiteLayout from '../../layouts/SiteLayout.astro';
import Viewer from '../../components/Viewer';
export function getStaticPaths() { return [{ params: { slug: 'showcase-1' } }]; }
const { slug } = Astro.params;
---
<SiteLayout title="Game explorer — Principles at War"><Viewer slug={slug!} client:load /></SiteLayout>
